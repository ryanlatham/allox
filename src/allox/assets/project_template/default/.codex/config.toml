# allox:begin managed
# This project uses allox to keep Codex as the only visible runtime.
# Opening the repo in Codex should be enough for normal development.
# Use the allox project runtime when task bootstrap, review gates, closeout,
# or cleanup are needed behind the scenes.
# Avoid risky default approvals or network assumptions in this file.
# allox:end managed

# Add project-local Codex configuration below this line.
