# Closeout Task Skill

- Run final checks and reviewer passes as needed.
- Write closeout artifacts with `allox project closeout --project .`.
- Clean scratch files with `allox project cleanup --project .`.
