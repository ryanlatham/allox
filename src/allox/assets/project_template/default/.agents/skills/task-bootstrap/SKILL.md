# Task Bootstrap Skill

- Call `python3 scripts/ai/bootstrap_task.py --title "<task title>"`.
- Review the generated task, plan, and progress files under `ai/`.
- Fill in the task scope before implementation begins.
