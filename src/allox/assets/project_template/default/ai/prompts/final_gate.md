# Final Gate Prompt

Read `AGENTS.md`, then review the final closeout packet at:

`[[PACKET_PATH]]`

Return JSON only using the shared findings shape defined in `ai/schemas/review_findings.schema.json`.
